import { describe, it, expect, vi } from 'vitest'
import MySettingsView from '@/views/profile/MySettingsView.vue'
import { mount } from '@vue/test-utils'

describe('MySettingsView', () => {
  it('renders the component', () => {
    expect(true).toBe(true)

  })
})

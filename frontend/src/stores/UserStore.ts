import { ref } from "vue";

const email = ref('');
const firstName = ref('');
const lastName = ref('');
const username = ref('');
const password = ref('');
const role = ref('');

export const useUserStore = defineStore('user', () => {

  

})